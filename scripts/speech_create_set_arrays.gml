///speech_create_set_arrays()

var length = array_length_1d(string_array);

time = zeros(length);
rotation = zeros(length);
x_offset = zeros(length);
y_offset = zeros(length);
colour = zeros(length);
font = zeros(length);
e_rotation = zeros(length);
e_shake = zeros(length);
e_gradient = zeros(length);
e_shadow = zeros(length);
letter_x = zeros(length);
letter_y = zeros(length);
